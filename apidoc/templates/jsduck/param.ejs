<%
  var name = param.name;
  if ('optional' in param && param.optional === true) {
    name = '[' + name;
    if ('default' in param) {
      name += '=' + JSON.stringify(param['default']);
    }
    name = name + ']';
  }
-%>
 * @param {<%- param.type.join('/') %>} <%- name %> <%- param.summary -%>
