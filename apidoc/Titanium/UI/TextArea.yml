---
name: Titanium.UI.TextArea
description: |
    A multiline text field that supports editing and scrolling. 
     
    Use the <Titanium.UI.createTextArea> method to create a text area. 
extends: Titanium.UI.View
since: "0.8"
methods:
  - name: blur
    description: Forces the text area to lose focus.
  - name: focus
    description: Forces the text area to gain focus.
  - name: hasText
    description: Returns `true` if the text area contains text, `false` if it is empty.
events:
  - name: blur
    description: Fired when the text area loses focus.
    properties:
      - name: value
        description: Value of the text area at the time of the event.
  - name: change
    description: Fired when the text area value changes.
    properties:
      - name: value
        description: New value of the text area.
  - name: focus
    description: Fired when the text area gains focus.
    properties:
      - name: value
        description: Value of the text area at the time of the event.
  - name: return
    description: Fired when the return key is pressed on the keyboard.
    properties:
      - name: value
        description: Value of the text area at the time of the event.
  - name: selected
    description: Fired when text in the text area is selected.
    properties:
      - name: range
        description: A range of text. The range is an object with the properties `location` and `length`.
properties:
  - name: autoLink
    description: Automatically convert text within this area to clickable links. Specify which type of items should be converted using the `AUTODETECT` constants defined in <Titanium.UI>.
    platforms: [iphone, ipad]
    type: Number
  - name: autocapitalization
    description: >
        Specifies how text should be capitalized during typing. Specify one of the following constants from <Titanium.UI>: [TEXT_AUTOCAPITALIZATION_NONE](Titanium.UI.TEXT_AUTOCAPITALIZATION_NONE), [TEXT_AUTOCAPITALIZATION_WORDS](Titanium.UI.TEXT_AUTOCAPITALIZATION_WORDS), [TEXT_AUTOCAPITALIZATION_SENTENCES](Titanium.UI.TEXT_AUTOCAPITALIZATION_SENTENCES), or [TEXT_AUTOCAPITALIZATION_ALL](Titanium.UI.TEXT_AUTOCAPITALIZATION_ALL).
    platforms: [android]
    type: Number
  - name: editable
    description: Is the text area editable?
    type: Boolean
  - name: enabled
    description: Is the text area enabled?
    type: Boolean
  - name: keyboardToolbar
    description: Array of toolbar button objects to be used when the keyboard is displayed.
    type: Array<Object>
  - name: keyboardToolbarColor
    description: Color of the keyboard toolbar.
    type: String
  - name: keyboardToolbarHeight
    description: Height of the keyboard toolbar.
    type: Number
  - name: suppressReturn
    description: Should the return key be suppressed during entry?
    type: Boolean
  - name: value
    description: Value of the text area.
    type: String
  - name: scrollable
    description: Can the text area be scrolled?
    platforms: [iphone, ipad]
    type: Boolean
    default: true

examples:
  - title: Text Area with Custom Keyboard Toolbar
    example: |
  
        Both Text Areas and Text Fields can control the buttons displayed in a button bar above the keyboard when it's visible.
    
        Example using a custom keyboard toolbar:
    
            var textfield = Titanium.UI.createTextField({
                color:'#336699',
        	    value:'Focus to see keyboard w/ toolbar',
        	    height:35,
        	    width:300,
        	    top:10,
        	    borderStyle:Titanium.UI.INPUT_BORDERSTYLE_ROUNDED,
        	    keyboardToolbar:[flexSpace,camera, flexSpace,tf,flexSpace, send,flexSpace],
        	    keyboardToolbarColor: '#999',	
        	    keyboardToolbarHeight: 40,
            });

  - title: Basic Text Area with Customizations
    example: |
        This example creates a highly customized text area.
        
            var ta1 = Titanium.UI.createTextArea({
            	value:'I am a textarea',
            	height:70,
            	width:300,
            	top:60,
            	font:{fontSize:20,fontFamily:'Marker Felt', fontWeight:'bold'},
            	color:'#888',
            	textAlign:'left',
            	appearance:Titanium.UI.KEYBOARD_APPEARANCE_ALERT,	
            	keyboardType:Titanium.UI.KEYBOARD_NUMBERS_PUNCTUATION,
            	returnKeyType:Titanium.UI.RETURNKEY_EMERGENCY_CALL,
            	borderWidth:2,
            	borderColor:'#bbb',
            	borderRadius:5
            });
        
        
