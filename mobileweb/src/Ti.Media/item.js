(function(api){
	// Properties
	Ti._5.prop(api, 'albumArtist');

	Ti._5.prop(api, 'albumTitle');

	Ti._5.prop(api, 'albumTrackCount');

	Ti._5.prop(api, 'albumTrackNumber');

	Ti._5.prop(api, 'artist');

	Ti._5.prop(api, 'artwork');

	Ti._5.prop(api, 'composer');

	Ti._5.prop(api, 'discCount');

	Ti._5.prop(api, 'discNumber');

	Ti._5.prop(api, 'genre');

	Ti._5.prop(api, 'isCompilation');

	Ti._5.prop(api, 'lyrics');

	Ti._5.prop(api, 'mediaType');

	Ti._5.prop(api, 'playCount');

	Ti._5.prop(api, 'playbackDuration');

	Ti._5.prop(api, 'podcastTitle');

	Ti._5.prop(api, 'rating');

	Ti._5.prop(api, 'skipCount');

	Ti._5.prop(api, 'title');

})(Ti._5.createClass('Titanium.Media.Item'));