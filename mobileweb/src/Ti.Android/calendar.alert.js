(function(api){
	// Properties
	Ti._5.prop(api, 'alarmTime');

	Ti._5.prop(api, 'begin');

	Ti._5.prop(api, 'end');

	Ti._5.prop(api, 'eventId');

	Ti._5.prop(api, 'id');

	Ti._5.prop(api, 'minutes');

	Ti._5.prop(api, 'state');

})(Ti._5.createClass('Titanium.Android.Calendar.Alert'));